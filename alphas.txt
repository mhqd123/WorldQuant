rank(-(close - vwap) / ts_mean(high - low, 30)) * rank(volume / ts_mean(volume, 25)) / (1 + ts_std_dev(returns, 40))
rank(-(close - ts_mean(vwap, 3)) / ts_mean(high - low, 20)) * rank(volume / ts_mean(volume, 30))
rank(-(close - vwap) / ts_mean(high - low, 20)) * rank(volume / ts_mean(volume, 45)) / (1 + ts_std_dev(returns, 30))
rank(-(ts_mean(close, 3) - vwap) / ts_mean(high - low, 20)) * rank(volume / ts_mean(volume, 30))
rank(-(close - vwap) / (1 + ts_std_dev(high - low, 20))) * rank(volume / ts_mean(volume, 30))
